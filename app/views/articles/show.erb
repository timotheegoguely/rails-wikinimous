<div class="row">
  <div class="col-11 push-center">

    <h3><%= @article.title %></h3>
    <%= Kramdown::Document.new(@article.content).to_html.html_safe %>
    <%= link_to "Edit", edit_article_path(@article), :class => "button small", :role => "button" %>
    <%= link_to "Destroy", articles_path, :class => "button small outline", :role => "button", :data => "Are you sure?" %>

  </div>
</div>
